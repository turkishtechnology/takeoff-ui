import Playground from '../../components/Playground';
import tooltipConfigData from './tooltipPlaygroundConfig.json';
import { TkTooltip, TkButton } from '@takeoff-ui/react';
import Variants from './Examples/Variants';
import Position from './Examples/Position';

### Playground

<Playground
  configs={[tooltipConfigData]}
  componentMap={{ TkTooltip, TkButton }}
/>

## Variants

Provides 7 different variants `dark`, `white`, `info`, `success`, `danger`,
`warning`, `neutral` via `variant` property.

<Variants />

## Position

Establishes 12 different placement position `top-start`, `top`, `top-end`,
`right-start`, `right`, `right-end`, `bottom-start`, `bottom`, `bottom-end`,
`left-start`, `left`, `left-end` via `position` property.

<Position />
