import Basic from './Examples/Basic';
import Size from './Examples/Size';
import State from './Examples/State';
import Filter from './Examples/Filter';
import CustomItem from './Examples/CustomItem';
import Multiple from './Examples/Multiple';
import Loading from './Examples/Loading';
import GroupedOptions from './Examples/GroupedOptions';
import ChipOptions from './Examples/ChipOptions';
import Icon from './Examples/Icon';
import SelectAll from './Examples/SelectAll';
import VisibleItemCount from './Examples/VisibleItemCount';
import PanelHeader from './Examples/PanelHeader';
import OptionValueKey from './Examples/OptionValueKey';
import Clearable from './Examples/Clearable';
import OptionDisabled from './Examples/OptionDisabled';
import { TkAlert } from '@takeoff-ui/react';
import Playground from '../../components/Playground/Playground';
import selectPlaygroundConfig from './selectPlaygroundConfig.json';
import { TkSelect } from '@takeoff-ui/react';

export const TkSelectWithOptions = props => {
  const options = [
    { label: 'Option 1', value: 'opt1' },
    { label: 'Option 2', value: 'opt2' },
    { label: 'Option 3', value: 'opt3' },
  ];
  return <TkSelect options={options} {...props} />;
};

### Playground

<Playground
  configs={[selectPlaygroundConfig]}
  componentMap={{ TkSelect: TkSelectWithOptions }}
/>

## Basic

<Basic />

## Size

The "size" prop can be used with one of the sizes "small", "base", and "large".
The default value is "base".

<Size />

## State

<State />

## Filter

You can perform filtering either server-side or client-side using the filter
emit.

<Filter />

## Loading

You can use `loading` prop for waiting the response from the server-side.

<Loading />

## Custom Item

You can provide a template as an HTML string using the optionHtml prop.

<CustomItem />

## Panel Header

You can customize top of the select panel using `panelTopHtml` prop.

<PanelHeader />

## Multiple Select

<Multiple />

## Select All

You can add `selectAll` option and edit the label via `selectAllLabel`.

<TkAlert
  header="Note"
  variant="info"
  message="This feature works in conjunction with the `multiple` prop set to `true`."
  filledlight
/>
<br />
<SelectAll />

## Visible Item Count

Controls how many selected items are displayed before showing "+N others" in
multiple selection mode.

<TkAlert
  header="Note"
  variant="info"
  message="This feature works in conjunction with the `multiple` prop set to `true`."
  filledlight
/>
<br />
<VisibleItemCount />

## OptionValueKey

Allows usage of Key for option values.

<OptionValueKey />

## Icon

You can use the `icon` prop to add an icon to the select input.

<Icon />

## Clearable

This example clears the input of the select.

<Clearable />

<TkAlert
  header="Caveat"
  variant="warning"
  message="Clearable button is hidden when readonly."
  filledlight
/>

## Grouped Options

Display options under section headers using `groupNameKey` and
`groupOptionsKey`.

<GroupedOptions />

## Chip Options

Sets options for all chips rendered in `multiple` selection mode.

<ChipOptions />

## Option Disabled

Sets disabled state for specific options using `optionDisabled` prop.

<OptionDisabled />
