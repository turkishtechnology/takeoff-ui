import Default from './Examples/Default';
import Label from './Examples/Label';
import Variants from './Examples/Variants';
import Sizes from './Examples/Sizes';
import Orientation from './Examples/Orientation';
import Playground from '../../components/Playground/Playground';
import spinnerPlaygroundConfig from './spinnerPlaygroundConfig.json';
import { TkSpinner } from '@takeoff-ui/react';

### Playground

<Playground configs={[spinnerPlaygroundConfig]} componentMap={{ TkSpinner }} />

## Default

The default usage of spinner.

<Default />

## Label

Label of the spinner is defined with the `label` property.

<Label />

## Variants

Demonstrates various spinner types `rounded`, `dots`, `lines`, `pulse`,
`three-dots` and `loader` available via the `type` property.

<Variants />

## Sizes

Demonstrates various spinner sizes available via the `size` property.

<Sizes />

## Orientation

Provides both `horizontal` and `vertical` orientations via `orientation`
property.

<Orientation />
