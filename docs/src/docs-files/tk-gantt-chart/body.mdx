import Basic from './Examples/Basic';
import ViewType from './Examples/ViewType';
import TaskClick from './Examples/TaskClick';
import Customization from './Examples/Customization';

## Basic

This demo shows a basic Gantt chart with tasks containing start date, end date,
progress, and custom colors. Each task is displayed as a horizontal bar on the
timeline.

<Basic />

## View Type

The Gantt chart supports three different view types: `day`, `week`, and `month`.
Use the built-in view switcher to change between different time scales.

<ViewType />

## Task Click Event

Handle task click events to show task details or perform actions when a user
clicks on a task bar.

<TaskClick />

## Customization

Customize the Gantt chart appearance with options like `rowHeight`,
`showProgress`, `showTooltip`, and `locale` for internationalization support.

<Customization />
