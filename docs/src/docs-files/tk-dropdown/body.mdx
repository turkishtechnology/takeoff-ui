import Playground from '../../components/Playground';
import dropdownConfigData from './dropdownPlaygroundConfig.json';
import { TkDropdown, TkButton } from '@takeoff-ui/react';
import Basic from './Examples/Basic';
import Size from './Examples/Size';
import Position from './Examples/Position';
import State from './Examples/State';
import CustomItem from './Examples/CustomItem';
import ItemClick from './Examples/ItemClick';

### Playground

<Playground
  configs={[dropdownConfigData]}
  componentMap={{ TkDropdown, TkButton }}
/>

## Basic

Demonstrates how to open and close the dialog with simple content.

<Basic />

## Size

You can set the size of the dropdown using the `size` prop. The available sizes
are `large` , `base`, and `small`. If no size is specified, the default size is
`base`.

<Size />

## Position

This demo demonstrates how to use the `position` prop to determine the opening
direction of the dropdown component. The `position` prop can accept the
following values: `bottom`, `bottom-end`, `bottom-start`, `left`, `left-end`,
`left-start`, `right`, `right-end`, `right-start`, `top`, `top-end`,
`top-start`.

<Position />

## Option Disable

You can pass disable: true for the option you want to disable.

<State />

## Custom Item

You can add custom items using optionHtml prop.

<CustomItem />

## Item Click

You can click an item from dropdown using tk-item-click event.

<ItemClick />
