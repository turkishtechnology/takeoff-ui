import Playground from '../../components/Playground';
import tabsConfigData from './tabsPlaygroundConfig.json';
import { TkTabs, TkTabsItem } from '@takeoff-ui/react';
import Variants from './Examples/Variants';
import Type from './Examples/Type';
import Sizes from './Examples/Sizes';
import Orientation from './Examples/Orientation';
import SpreadHeaders from './Examples/SpreadHeaders';
import AlignHeaders from './Examples/AlignHeaders';
import Closable from './Examples/Closable';
import Controlled from './Examples/Controlled';
import Programmatically from './Examples/Programmatically';
import BadgeOptions from './Examples/BadgeOptions';

### Playground

<Playground configs={[tabsConfigData]} componentMap={{ TkTabs, TkTabsItem }} />

## Variants

Provides 3 different variant `primary`, `info`, `neutral` via `variant`
property.

<Variants />

## Type

Establishes various tab styles `basic`, `divided`, `compact`, `expanded`
available via the `type` property.

<Type />

## Sizes

Provides 5 different sizes `xxsmall`, `xsmall`, `small`, `base`, `large`
available via `size` property.

<Sizes />

## Orientation

Provides both `horizontal` and `vertical` orientations via `orientation`
property.

<Orientation />

## Spread Headers

Determines whether the tab headers will spread evenly across the horizontal
space.

<SpreadHeaders />

## Align Headers

Tabs headers can aligned as `start`, `center` or `end` via `align-headers`
property.

<AlignHeaders />

## Closable

Tabs can be closed separately via `is-closable` property.

<Closable />

## Controlled

Tabs can be controlled via the `controlled` property.

<Controlled />

## Programmatic Control

Tabs can be controlled programmatically using the `activeIndex` prop.

<Programmatically />

## Badge Options

Tabs can display badges with various styles via `badged`, `badgeLabel`,
`badgeCount` and `badgeOptions` properties.

<BadgeOptions />
