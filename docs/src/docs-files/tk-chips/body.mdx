import Variant from './Examples/Variant';
import Type from './Examples/Type';
import Size from './Examples/Size';
import Icon from './Examples/Icon';
import IconPosition from './Examples/IconPosition';
import FullWidth from './Examples/FullWidth';
import Playground from '../../components/Playground';
import chipsConfigData from './chipsPlaygroundConfig.json';
import { TkChips } from '@takeoff-ui/react';

### Playground

<Playground configs={[chipsConfigData]} componentMap={{ TkChips: TkChips }} />

## Variant

The variant of the chips for styling.

<Variant />

## Type

This field specifies the design type of the component. "filled", "filledlight",
"outlined" designs are available

<Type />

## Size

The "size" prop can be used with one of the sizes "small", "base", and "large".
The default value is "base".

<Size />

## Icon

It can be used with icons. You should use Material Symbols icons.

<Icon />

## Icon Position

The `iconPosition` prop controls whether the icon appears to the left or right
of the label. The default value is `"left"`.

<IconPosition />

## FullWidth

The `fullWidth` prop makes the chips component expand to the full width of its
container.

<FullWidth />
