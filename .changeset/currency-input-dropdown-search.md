---
'@takeoff-ui/core': patch
---

Add a search box to the `tk-currency-input` dropdown, matching the one in
`tk-phone-input`. Typing filters the currency list by code, name or symbol; the
search term is cleared whenever the dropdown closes or a currency is selected.
