import Basic from "./Examples/Basic";
import Sizes from "./Examples/Sizes";
import Variants from "./Examples/Variants";
import WithImages from "./Examples/WithImages";
import WithBadges from "./Examples/WithBadges";
import AvatarGroupDemo from "./Examples/AvatarGroupDemo";
import {TkAlert} from "@takeoff-ui/react"

## Basic

A simple `TkAvatar` displaying initials or a default icon.

<Basic />

## Sizes

Demonstrates various avatar sizes available via the `size` property.

<Sizes />

## Variants

Showcases different variant levels (variants) of the avatar using the `variant` property.

<TkAlert header="Note" variant="warning" message="This feature works in conjunction with the `background` prop set to `solid`." filledlight />
<br/>
<Variants />

## Avatar with Images

Displays how to use images inside the `TkAvatar` component.

<WithImages />

## Avatar with Badges

Illustrates adding badges to avatars using the `badge` and `badgeStatus` properties.

<WithBadges />

## Avatar Group

Shows how to group multiple avatars together using the `TkAvatarGroup` component.

<AvatarGroupDemo />