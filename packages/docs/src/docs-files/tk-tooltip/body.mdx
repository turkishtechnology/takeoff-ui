import Variants from "./Examples/Variants";
import Position from "./Examples/Position"

## Variants

Provides 7 different variants `dark`, `white`, `info`, `success`, `danger`, `warning`, `neutral` via `variant` property.

<Variants />

## Position

Establishes 4 different placement position `top`, `right`, `left`, `bottom` via `position` property.

<Position />
